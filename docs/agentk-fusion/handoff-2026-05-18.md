# Handoff Note — 2026-05-15 → 2026-05-18

**TL;DR**：在 5/15 一天內把 AgentK 會議模組的 schema + 行為層**全部嫁接到 Kway Dev 本地**，schema 純 additive、權限沒動。5/18 您回來不用退我任何 commit；想拿哪部分過去 AgentK 都可以照表抽。

---

## 設計前提（您點頭過的決策）

1. **Kway Dev 為主體**，AgentK 是補強來源
2. **只新增、不更新、不刪除欄位**；既有 `notification_note` / `external_id` / `portal_*` 與 AgentK 新欄位共存
3. **權限完全不動**：Kway Dev 的記憶權重邏輯靠 owner/admin/editor/viewer，AgentK 的 admin/owner/delegate/attendee 在 mapping 表轉譯，不改 schema
4. **5/26 demo 跑 AgentK**，不是我們系統；我這邊不做 AgentK.pen 視覺對齊

完整計畫 + 對照表：`docs/agentk-fusion/fusion-plan.md`

---

## Schema diff（4 個新 migration，全純加欄位）

| Migration | Target | Adds | Notes |
|---|---|---|---|
| `0032_meetings_agentk_alignment.sql` | `meetings` | description / is_locked / join_url / external_provider / external_event_id / external_event_url / sync_status / last_synced_at / updated_by_user_id（共 9） | 對齊 AgentK 的 Meeting domain。Backfill `is_locked=TRUE WHERE status='completed'`。 |
| `0033_meeting_notes_records_alignment.sql` | `meeting_notes` | action_items / ai_job_ids / task_ids（3 個 JSONB） | 對齊 AgentK `meeting_records` aggregate。我們保留 multi-version notes 模型；最新 version 等同 record。 |
| `0034_meeting_files_retention.sql` | `meeting_files` | deleted_at / soft_deleted_until / hard_delete_after / metadata | 對齊 assets 軟刪除 retention。`DELETE` 改 stamp 三欄；tokio 背景 worker 每小時掃 hard_delete_after 真刪。 |
| `0035_ai_jobs.sql` | new `ai_jobs` table | kind / provider / model / requested_by / meeting_id / status / chars / duration / prompt_hash | AgentK-aligned LLM call audit。每次 generate 寫一筆，ID 進 meeting_notes.ai_job_ids。 |

---

## 角色名對照

```
AgentK admin    →  Kway Dev owner   (workspace-wide highest)
AgentK owner    →  Kway Dev admin   (per-meeting primary)
AgentK delegate →  Kway Dev editor  (per-meeting deputy)
AgentK attendee →  Kway Dev viewer  (per-meeting member)
```

抽 AgentK code 過來時請把字串 literal 替換掉。

---

## Endpoint 對照（Kway Dev → AgentK）

| Kway Dev | AgentK 等價 | 備註 |
|---|---|---|
| `GET /meetings` | `GET /workspaces/{w}/meetings` | 我們已加 `visibility: 'full'\|'busy'` 對齊 busy masking |
| `GET /meetings/calendar` | `GET /workspaces/{w}/meetings/calendar` | 目前只回 count；要補完整 calendar entries 還有缺口 |
| `POST /meetings` | `POST /workspaces/{w}/meetings` | 同樣支援 description / join_url / external_provider |
| `GET /meetings/:id` | `GET /workspaces/{w}/meetings/{m}` | |
| `PATCH /meetings/:id` | `PATCH /workspaces/{w}/meetings/{m}` | 含 is_locked auto rule on status='completed' |
| `POST /meetings/:id/reopen` | `PATCH is_locked=false` | 拆出獨立 endpoint，role-guard 同 delete |
| `DELETE /meetings/:id` | 無（AgentK 用 cancelled + retention） | 我們保留實刪；附 portal cancel 連動 |
| `POST /meetings/:id/notes/generate` | `POST /meetings/{m}/record/generate` | 已接 HermesClient，寫入 ai_jobs |
| `POST /meetings/:id/notes/sync-tasks` | `POST /meetings/{m}/record/tasks/sync` | action_items → project_tasks 去重 |
| `WebSocket /ws/meetings` | `meeting.*` events on AgentK WS | tokio::sync::broadcast 內存 fan-out，未接 pg LISTEN |

---

## 我們**獨有**的（AgentK 沒有）

- **凱衛 portal 整合**：`portal_book.rs` + `kway_portal/features/meeting_book.py`
  - create/send_invitations → 自動同步預約到 crm.kway.com.tw
  - delete → 自動取消（best-effort）
  - 既有 portal_booked_at / portal_book_error / external_creator_name 欄位繼續用
- **完整 i18n 既有 importance / recurrence / timezone / all_day**
- **meeting_task_impacts** 任務影響詳細紀錄（progress_from/to）— 與 task_ids 並存

---

## **AgentK 有、我們還沒做**的（您回來可以接著開）

| 項目 | 為什麼還沒做 |
|---|---|
| Dedicated `room_id` 1:1 binding | 我們沒 room 概念，加空欄位無意義 |
| Generic `assets` table | 牽動 audio_transcripts 等，超出 scope |
| `meeting_participants.updated_at` | 純審計，5/26 demo 不需要 |
| Workspace scope（vs 我們的 organization_id / project_id） | tenant 模型差太多，不是這次能解的 |
| Realtime 跨節點 pg LISTEN/NOTIFY | 我們現在 in-process broadcast 夠用；scale out 時再做 |
| Real email dispatch | deferred_backlog #33；目前 `invitations_sent_at` 只是 status flip |
| 線上會議連結自動產生（Webex/Teams/Meet OAuth） | deferred_backlog #32；目前只記 provider symbol + 手填 join_url |

---

## Commits 在 5/15 期間的時序

```
9550452  feat(meetings): delete + creator_name + 近期 filter + autocomplete safety
5fafdca  fix(meetings): portal book + cancel end-to-end works  ← 5/14 修 5/15 測通
9f1acba  docs(agentk-fusion): plan + reference diagrams
4a10dd1  feat(meetings): AgentK schema alignment (5/18-22 batch)
63149d2  feat(meetings): wire AI minutes to action_items aggregate
f2f5de7  feat(meetings): task-sync + ai_jobs audit + realtime WS broadcast
```

全部在 `origin/fsc` HEAD `f2f5de7`，可單一 cherry-pick 或全部 merge。

---

## 我需要您確認的

1. 5/26 demo 那天，這份 schema 改動是否要 PR 過 AgentK 那邊？或是只當補強，AgentK demo 仍用 AgentK 原 schema？
2. `ai_jobs` 表設計簡化版（只記 metadata，不存 prompt 全文）對齊 AgentK 需求嗎？AgentK 那邊未來補 `prompt`/`response` 全文時，我們再 ALTER ADD COLUMN 即可
3. WS event 命名 `MeetingEvent::{Created,Updated,Scheduled,Ended,Cancelled,LockChanged,RecordUpdated,Deleted}` 與您 AgentK 用的命名是否一致？不一致請告知，我這邊加 alias 序列化

---

## 跑起來

```powershell
# 1. 應用 migrations
psql ... -f backend/migrations/0032_meetings_agentk_alignment.sql
psql ... -f backend/migrations/0033_meeting_notes_records_alignment.sql
psql ... -f backend/migrations/0034_meeting_files_retention.sql
psql ... -f backend/migrations/0035_ai_jobs.sql

# 2. Build & run
cargo build --release
.\target\release\kway-dev-backend.exe  # or use cmd //c due to WDAC

# 3. Smoke test
按 docs/agentk-fusion/dry-run.md 逐項打勾
```

有任何要追問的細節 Slack/discord 隨時找我。
